import 'package:flutter/material.dart';

class BirthdaysTab extends StatelessWidget {
  const BirthdaysTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text("It is a birthday tab"),
      ),
    );
  }
}
